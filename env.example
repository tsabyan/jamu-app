# Environment Variables for Supabase

# Get these values from your Supabase project settings:
# https://app.supabase.com/project/_/settings/api

# Supabase URL (found in Project Settings > API)
NEXT_PUBLIC_SUPABASE_URL=your-project-url.supabase.co

# Supabase Anon/Public Key (found in Project Settings > API)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional: Supabase Service Role Key (for admin operations, keep this secret!)
# Only use this on the server side, NEVER expose to the client
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
